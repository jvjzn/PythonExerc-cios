larg = float(input('Insira a largura de uma parede em metros: '))
alt = float(input('Insira a altura de uma parede em metros: '))
print ('A área total da parede: {:.2f}m².\nSerão necessários {:.2f}L de tinta.'.format(larg*alt,(larg*alt)/2))